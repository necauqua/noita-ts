{
  "name": "@noita-ts/create-mod",
  "version": "0.1.1",
  "description": "Scaffold a new noita-ts mod",
  "type": "module",
  "bin": {
    "create-mod": "dist/index.js"
  },
  "files": [
    "dist",
    "template"
  ],
  "scripts": {
    "prepublishOnly": "rm -rf dist && npm run build",
    "build": "tsc"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.3.2",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "@clack/prompts": "^1.0.1"
  }
}
